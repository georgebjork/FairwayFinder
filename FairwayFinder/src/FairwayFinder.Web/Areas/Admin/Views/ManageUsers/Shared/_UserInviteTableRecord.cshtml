@model FairwayFinder.Core.Features.Admin.UserManagement.Models.UserInvitation

<tr id="invite-@Model.id" class="border-b border-base-100">
    <td>@Model.sent_to_email</td>
    <td>@Model.InviteUrl</td>
    <td>
        <div class="flex justify-end">
            <button class="" type="button" onclick="this.innerText='Revoking invite...'; this.disabled=true;"
                    hx-trigger="click"
                    hx-post="/revoke-invite/@Model.invitation_identifier"
                    hx-swap="outerHTML"
                    hx-target="#invite-@Model.id">
                                        
                <i class="fa-solid fa-trash text-error pe-2"></i>
            </button>
        </div>
    </td>
</tr>